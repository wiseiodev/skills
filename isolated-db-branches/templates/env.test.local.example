# Test database (used by Vitest, Playwright). Localhost so tests are cheap and resettable.
# Bring up with: docker compose -f docker-compose.test.yml up -d
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/test_db"
DATABASE_URL_UNPOOLED="postgresql://postgres:postgres@localhost:5432/test_db"
